import classNames from 'classnames';

/**
 * Thin wrapper around `classnames` so components have a single, short
 * import (`cn`) for conditionally combining Tailwind classes.
 */
export const cn = classNames;
